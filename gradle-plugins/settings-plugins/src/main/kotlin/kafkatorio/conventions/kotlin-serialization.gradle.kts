package kafkatorio.conventions

plugins {
  kotlin("plugin.serialization")
}

// No config required or available. This convention plugin is only required because Gradle gets
// confused about the version.
