version: "3.9"

services:

  kafkatorio-web-map:
    image: ${REGISTRY_HOST}/dev.adamko.kafkatorio/web-map:${KAFKATORIO_VERSION}
    container_name: kafkatorio-web-map
    restart: unless-stopped
    build:
      context: ".."
      dockerfile: ./docker/Dockerfile
    ports:
      - "12081:80"
    networks:
      - "kafkatorio-nw"
      - "traefik-nw"
    labels:
      - "traefik.enable=true"

networks:
  kafkatorio-nw:
    name: "kafkatorio-nw"
    external: true
  traefik-nw:
    name: "traefik-nw"
    external: true
