# Kafkatorio server config

jwt-secret: "correct horse battery staple"

# Define the servers that have the Kafkatorio Mod installed.
kafkatorio-servers:
  # The key is the short ID, used to uniquely identify a server (used in URLs).
  # The value is a description.
  short-id-123:
    id: "server1"
    description: "description of server 1"
  another-id-456:
    id: "server2"
    description: "Name of another server"
